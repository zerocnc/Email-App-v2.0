import { MyButtonDirective } from './my-button.directive';

describe('MyButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MyButtonDirective();
    expect(directive).toBeTruthy();
  });
});
