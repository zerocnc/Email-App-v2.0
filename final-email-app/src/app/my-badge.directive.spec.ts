import { MyBadgeDirective } from './my-badge.directive';

describe('MyBadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new MyBadgeDirective();
    expect(directive).toBeTruthy();
  });
});
